"use strict";

/** MVP — códigos de bloqueio / sinalização estrutural (Fase 4.9.6). */
const STRUCTURAL_BLOCKER_CODES = {
  STRUCTURAL_AST_CORRUPT: "STRUCTURAL_AST_CORRUPT",
  STRUCTURAL_FORMATTER_DRIFT: "STRUCTURAL_FORMATTER_DRIFT",
  STRUCTURAL_MULTI_FILE_CASCADE: "STRUCTURAL_MULTI_FILE_CASCADE",
  STRUCTURAL_UNSAFE_DELETE_NODE: "STRUCTURAL_UNSAFE_DELETE_NODE",
  STRUCTURAL_LOW_CONFIDENCE: "STRUCTURAL_LOW_CONFIDENCE",
};

const MVP_CODES_LIST = Object.freeze(Object.values(STRUCTURAL_BLOCKER_CODES));

module.exports = {
  STRUCTURAL_BLOCKER_CODES,
  MVP_CODES_LIST,
};
