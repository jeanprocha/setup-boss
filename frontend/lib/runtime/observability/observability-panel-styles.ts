/** Tipografia padrão do painel Observabilidade (Mission Control). */
export const OBSERVABILITY_FONT_CLASS = "text-[8px]";

/** Raiz do conteúdo da aba Observabilidade (3 abas planas). */
export const OBSERVABILITY_PANEL_ROOT_CLASS = `flex min-h-0 flex-1 flex-col ${OBSERVABILITY_FONT_CLASS} leading-tight`;
