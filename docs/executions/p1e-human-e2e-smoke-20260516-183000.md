# P1e — Smoke E2E Humano

**Data:** 2026-05-16  
**Escopo:** Validação operacional ponta a ponta (API + checklist UI). Sem features novas.

## 1. Fluxo testado

```text
Preparação (stack + projeto git + docs/.IA)
  → Nova atividade (POST /runs)
  → Clarificação (perguntas → respostas → refine → approve)
  → Estratégia (ready / generate)
  → Execução (POST /execute → execution_completed)
  → Observabilidade (events + SSE + runtime-observability)
```

Validação **API automatizada** cobre o pipeline completo. Validação **UI humana** documentada como checklist manual (secção 5).

## 2. Passos executados

| # | Passo | Resultado |
|---|--------|-----------|
| 1 | `dev:stack` activo; health 200 | OK |
| 2 | `docs/.IA/index.md` no repo Setup-Boss | OK |
| 3 | Restart daemon (`daemon stop` + `start`) para código P1b/P1e | Necessário |
| 4 | Projeto demo temp com `git init` + seed SPEC tracked | OK |
| 5 | `POST /projects/register` | OK |
| 6 | `GET /projects/:id/governance` (sem 400) | OK (`readiness=ready`) |
| 7 | `POST /runs` (skipLlm) | OK 201 |
| 8 | Metadata honesto: `job=completed`, `initialState=clarification_required`, `uiState≠success` | OK (P1c) |
| 9 | Clarificação completa | OK |
| 10 | Estratégia ready | OK |
| 11 | Execução → `execution_completed` | OK |
| 12 | Events + SSE sample | OK |
| 13 | `GET /runs/:id/runtime-observability` | OK |

### Comandos / evidência

```bash
# Testes regressão (fix P1e)
node --test scripts/daemon/lib/pre-run-observability.test.js
# → 8/8 pass

# Smoke live (daemon em :3210)
node scripts/smoke/p1e-human-e2e-live-smoke.js
# → 17/17 pass (após restart daemon + projeto demo)
```

Script novo: `scripts/smoke/p1e-human-e2e-live-smoke.js` (não arranca daemon; usa stack existente).

## 3. Problemas encontrados

| ID | Severidade | Descrição |
|----|------------|-----------|
| E1 | **Bloqueador (corrigido)** | `POST /runs` → 500 `compact is not defined` em `traceKnowledgeBootstrapFailed` quando validação KB falha. Variável `compact` usada sem declaração. |
| E2 | Operacional | Daemon com ~56min uptime não tinha rota `GET /projects/:id/governance` → 400 genérico. **Requer restart** após deploy local. |
| E3 | Esperado | Registar **Setup-Boss como projectRoot** → `PROJECT_ROOT_UNRESOLVED` / governance `blocked`. Validação exige clone do cliente, não o monorepo da ferramenta. |
| E4 | Menor | Smoke inicial usava path `/observability` em vez de `/runtime-observability` (404). Corrigido no script. |

## 4. Correções mínimas aplicadas

### `scripts/daemon/lib/pre-run-observability.js`

Em `traceKnowledgeBootstrapFailed`, adicionado:

```javascript
const compact = compactDiagnosticEvent(structured);
```

antes de `appendRuntimeTrace`, para `groupedDiagnostics` no metadata.

### `scripts/smoke/p1e-human-e2e-live-smoke.js` (novo)

- Cria projeto demo com git + `docs/.IA` tracked (seed SPEC).
- Exercita fluxo API completo contra `:3210`.
- Valida metadata P1c e endpoints de observabilidade.

## 5. Checklist UI manual (pendente operador)

Com `npm run dev:stack`, projeto **cliente** registado (não Setup-Boss), `localStorage` limpo ou reconciliado (P1a):

| Área | Verificar |
|------|-----------|
| **Nova atividade** | Não perde tela; sem projectId órfão; sem governance 400; loading no botão; run seleccionada na sidebar |
| **Clarificação** | Perguntas visíveis; respostas gravam; sidebar **não** “Concluída” (P1c) |
| **Estratégia** | CTA visível; gerar estratégia; estado coerente |
| **Execução** | Botão executar quando permitido; bloqueios com mensagem; timeline/logs actualizam |
| **Observabilidade** | Scroll nos logs; cards legíveis; diagnostics claros; sem flicker (P1d); Network sem polling excessivo |
| **Final** | “Concluída” só após execução real; erros legíveis; copy diagnostics |

## 6. Bugs pendentes

- Nenhum bloqueador de API após fix E1 + restart daemon.
- **UI humana** não exercitada nesta sessão (sem browser automation) — seguir checklist secção 5.
- Projetos antigos no registry apontam para `%TEMP%` possivelmente inexistentes → podem poluir lista (pré-existente; fora P1e).

## 7. Próximos passos recomendados

1. Operador: percorrer checklist UI (secção 5) com projeto real Bitbucket + `docs/.IA` versionada.
2. Opcional: `npm run smoke:mvp-web-ui-e2e` em CI (daemon isolado) como regressão complementar.
3. Considerar aviso na UI ao registar projectRoot = Setup-Boss (já bloqueado na API).
4. Após `daemon stop/start` ou `dev:stack` restart, confirmar governance + intake no browser.

## 8. Efeito esperado na UX (P1a–P1d integrados)

- Shell não mantém project/run órfãos (P1a).
- Governance sem 400 para projectId válido (P1b).
- Sidebar honesta durante clarificação/estratégia (P1c).
- Menos refetch/flicker com SSE activo (P1d).
- Erros pre-run com diagnostics estruturados (fix E1 — sem 500 opaco).

## Resumo executivo

| Métrica | Valor |
|---------|--------|
| Smoke API live | **17/17 pass** |
| Correções código | **1** (pre-run `compact`) |
| Restart daemon | **Obrigatório** após pull local |
| UI browser | **Checklist manual** — não automatizado nesta execução |
